<!DOCTYPE html>
<html lang="en">

<head>
    <title>books</title>
</head>

<body>
    <h1>BOOKS</h1>
    <ul>
        <% books.forEach(book => { %>
            <li> 
                <p><%= book.volumeInfo.title %></p>
                <img src="<%=  book.volumeInfo.imageLinkes.thumbnail %>">
                <p> <%= data.volumeInfo.description %> </p>
                <p> <% data.volumeInfo.authors %></p>
            </li>
        <% }) %>
    </ul>
</body>

</html>